<UserControl x:Class="anartsgame.views.MainMenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:anartsgame.viewmodels"
             xmlns:services="clr-namespace:anartsgame.services"
             Background="#0a0a0a">
    <UserControl.DataContext>
        <vm:MainMenuViewModel/>
    </UserControl.DataContext>

    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="300">
            <TextBlock Text="anarts"
                       FontFamily="pack://application:,,,/styles/Watc2.TTF#Watc"
                       FontSize="48"
                       Foreground="#00ff00"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,60"/>

            <Button Content="{Binding Source={x:Static services:LocalizationService.Instance}, Path=[MainMenu_NewGame]}"
                    Command="{Binding NewGameCommand}"
                    Style="{StaticResource MenuButtonStyle}"
                    Margin="0,0,0,15"/>

            <Button Content="{Binding Source={x:Static services:LocalizationService.Instance}, Path=[MainMenu_Continue]}"
                    Command="{Binding ContinueCommand}"
                    Style="{StaticResource MenuButtonStyle}"
                    IsEnabled="{Binding HasSaveGame}"
                    Margin="0,0,0,15"/>

            <Button Content="{Binding Source={x:Static services:LocalizationService.Instance}, Path=[MainMenu_Settings]}"
                    Command="{Binding SettingsCommand}"
                    Style="{StaticResource MenuButtonStyle}"
                    Margin="0,0,0,15"/>

            <Button Content="{Binding Source={x:Static services:LocalizationService.Instance}, Path=[MainMenu_Exit]}"
                    Command="{Binding ExitCommand}"
                    Style="{StaticResource MenuButtonStyle}"/>
        </StackPanel>
    </Grid>
</UserControl>
